# team-project
Create a simple Python API that manages a list of food items. The API should have endpoints to perform Create, Read, Update, Delete operations, storing the data in a simple Python list or dictionary
